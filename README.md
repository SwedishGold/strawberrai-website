# StrawberrAI Website

Lokal AI-wiki for svenska foretag.

## Usage

Open `index.html` in a browser, or serve with any static server:

```bash
npx serve .
```

## Development

Pure HTML/CSS -- no build step required.